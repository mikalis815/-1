public class ArraySum {
    public static void main(String[] args) {
        // Объявление и инициализация массива из 5 целых чисел
        int[] numbers = {2, 4, 6, 8, 10};
        
        // Переменная для хранения суммы, инициализируется нулем
        int sum = 0;
        
        // Цикл for для перебора всех элементов массива
        // i - счетчик цикла, начинается с 0 (первый элемент массива)
        // условие i < numbers.length - пока i меньше длины массива
        // i++ - увеличение i на 1 после каждой итерации
        for (int i = 0; i < numbers.length; i++) {
            // Добавляем текущий элемент к сумме
            // numbers[i] - обращение к элементу массива по индексу i
            sum += numbers[i];
        }
        
        // Вывод результата на экран
        System.out.println("Сумма элементов массива: " + sum);
    }
}
